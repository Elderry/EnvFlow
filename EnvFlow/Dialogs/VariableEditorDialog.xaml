<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="EnvFlow.Dialogs.VariableEditorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Edit Variable"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    CloseButtonClick="ContentDialog_CloseButtonClick">

  <StackPanel Spacing="12" MinWidth="400">
    <TextBlock x:Name="VariableNameLabel" Text="Variable Name" FontWeight="SemiBold"/>
    <TextBox x:Name="VariableNameTextBox"
                 PlaceholderText="Enter variable name"
                 TextChanged="TextBox_TextChanged"/>

    <TextBlock Text="Variable Value" FontWeight="SemiBold" Margin="0,8,0,0"/>
    <TextBox x:Name="VariableValueTextBox"
                 PlaceholderText="Enter variable value"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 MinHeight="100"
                 MaxHeight="300"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 TextChanged="TextBox_TextChanged"/>

    <StackPanel Orientation="Horizontal" Spacing="8">
      <Button Content="Browse Folder..." Click="BrowseFolder_Click"/>
      <Button Content="Browse File..." Click="BrowseFile_Click"/>
    </StackPanel>

    <TextBlock x:Name="ErrorTextBlock"
                   Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"
                   Margin="0,4,0,0"/>
  </StackPanel>
</ContentDialog>
